# RTG Review Backlog

Tracks non-critical follow-ups from `@code-reviewer`.

## Status legend

- `open` - not started
- `planned` - accepted, queued for future task/PR
- `done` - implemented
- `wontfix` - intentionally skipped with rationale

## Entries

| Date | Scope | Severity | Finding | Rationale | Next step | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-02-18 | Live chat updates | medium | Connectivity events can dominate streak budget when connectivity and chat-update streams are both hot (`src/ui/event_source.rs`) | Chat refresh events may be delayed under connectivity flapping | Add round-robin arbitration between pending connectivity and pending chat refresh and cover with mixed-burst fairness test | planned |
| 2026-02-18 | Live chat updates | medium | Chat updates monitor stops permanently on first `next_update` error (`src/telegram/chat_updates.rs`) | Transient network/MTProto error can disable live refresh until restart | Add retry with bounded backoff and terminate only on explicit fatal conditions | wontfix — TDLib update pipeline uses channel recv, not direct network calls; monitor stops only on channel close |
| 2026-02-18 | Live chat updates | minor | Update filter currently includes only `NewMessage`, `MessageEdited`, `MessageDeleted` (`src/telegram/chat_updates.rs`) | Some dialog metadata/unread transitions may not trigger immediate refresh | Expand relevance filter with dialog/unread-related update variants or add low-rate fallback refresh | planned |
| 2026-02-18 | Live chat updates | medium | Repeated `next_update` failures can spin and flood logs because monitor retries immediately (`src/telegram/chat_updates.rs`) | Sustained backend outage may cause elevated CPU usage and noisy logs | Add bounded retry backoff that resets after a successful update read | wontfix — TDLib update pipeline uses channel recv_timeout; no spin on network errors |
| 2026-02-18 | Live chat updates | medium | Chat-update signal channel is unbounded and currently propagates one refresh per signal (`src/ui/event_source.rs`, `src/usecases/bootstrap.rs`) | Bursty traffic can queue redundant refresh requests and increase memory/work | Switch to coalescing signal semantics or drain-and-coalesce per cycle | planned |
| 2026-02-18 | Live chat updates | minor | Fairness regression test currently validates tick progress only under bursty chat updates (`src/ui/event_source.rs`) | Input responsiveness under sustained update bursts is not explicitly guarded | Add bounded-cycle regression test that verifies `QuitRequested` can still surface during bursts | planned |
| 2026-02-18 | Logging to file | medium | Logging init currently calls `StorageLayout::ensure_dirs()`, coupling startup to creation of session/cache dirs (`src/infra/logging.rs`) | Logging should not fail due to unrelated storage subdirectories | Create only the log parent directory in logging init and keep session/cache creation in their own flows | planned |
| 2026-02-18 | Logging to file | medium | File appender uses `rolling::never` and can grow `~/.config/rtg/rtg.log` unbounded (`src/infra/logging.rs`) | Long-running debug sessions can consume disk space unexpectedly | Switch to rolling file strategy and define retention cleanup policy | done |
| 2026-02-18 | Logging to file | minor | Result of `LOG_GUARD.set(guard)` is ignored (`src/infra/logging.rs`) | Silent guard registration failure complicates diagnostics in re-init scenarios | Handle `set` result explicitly with debug trace | planned |
| 2026-02-20 | Chat list refresh optimization | medium | `OnceLock` cache for `DialogFetchScope` not cleared on `disconnect_and_reset()` (`src/telegram/auth.rs:47,253-258`) | Switching accounts after logout can use stale folder scope from previous account | Replace `OnceLock` with resettable cache or recreate backend on login | wontfix — `src/telegram/auth.rs` removed in TDLib migration |
| 2026-02-20 | Chat list refresh optimization | minor | Error fallback `DialogFetchScope::AllDialogs` is cached permanently on first API failure (`src/telegram/auth.rs:235-238`) | Transient network error at startup causes suboptimal fetching for entire session | Only cache successful results, retry on subsequent calls if initial failed | wontfix — `src/telegram/auth.rs` removed in TDLib migration |
| 2026-02-20 | Chat list refresh optimization | minor | No tests for `OnceLock` caching behavior in `list_chat_summaries()` (`src/telegram/auth.rs`) | Future refactoring could break caching optimization undetected | Add tests for cache population and reuse | wontfix — `src/telegram/auth.rs` removed in TDLib migration |
| 2026-02-20 | Chat list redesign | medium | Section headers are selectable - `build_chat_list_items` and `compute_visual_index` are tightly coupled (`src/ui/view.rs`) | Visual list contains non-selectable headers; architecture is fragile | Add comment documenting the coupling or refactor to use nested lists | open |
| 2026-02-20 | Chat list redesign | medium | Pinned count calculated twice: in `build_chat_list_items` and `compute_visual_index` (`src/ui/view.rs:76,100`) | Minor performance overhead and potential logic drift | Consider extracting helper struct `ChatListLayout` | open |
| 2026-02-20 | Chat list redesign | minor | Two timestamp formatting functions with similar names (`format_chat_timestamp`, `format_timestamp`) (`src/ui/view.rs`) | Could cause confusion; naming doesn't clearly distinguish purposes | Rename for clarity: `format_timestamp_for_chat_list`, `format_timestamp_for_message` | open |
| 2026-02-20 | Chat list redesign | minor | Style functions in `styles.rs` return new instances on each call | Very minor performance; could use const styles if ratatui supports it | Consider using const styles | open |
| 2026-02-20 | Chat list redesign | minor | Test helper `chat()` duplicated in 4 test modules | Maintenance burden when ChatSummary changes | Extract to shared test fixtures module | done |
| 2026-02-20 | Messages list visual | medium | No tests for `parse_message_media` and `parse_document_media` (`src/telegram/auth.rs:642-695`) | Regressions in media type detection could go unnoticed; functions have multiple branches | Add unit tests with mocked grammers types or integration tests | wontfix — `src/telegram/auth.rs` removed in TDLib migration; media mapping now in `tdlib_mappers.rs` |
| 2026-02-20 | Messages list visual fixes | medium | No unit tests for sender name fallback logic (`src/telegram/auth.rs:615-634`) | The sender fallback logic requires real Telegram client, hard to test directly | Extract sender resolution to testable function or add integration tests | wontfix — `src/telegram/auth.rs` removed in TDLib migration; sender resolution now in `tdlib_auth.rs` |
| 2026-02-20 | Messages list visual fixes | minor | `message_item()` function is 57 lines with two distinct paths (`src/ui/message_rendering.rs:118-175`) | Approaching complexity limit; grouped message rendering duplicates content line building | Extract grouped message rendering to separate helper function | open |
| 2026-02-20 | Messages list visual fixes | minor | Magic string for indent in `message_item()` (`src/ui/message_rendering.rs:120`) | Inline string with comment; could be a named constant | Define `MESSAGE_INDENT` constant at module level | open |
| 2026-02-20 | Pane navigation | minor | Tests directly access `selected_index` field to set up scenarios (`src/domain/open_chat_state.rs:212,272,284`) | Bypasses public API; tests may not catch issues if internal representation changes | Add `#[cfg(test)]` helper method for setting selection in tests | open |
| 2026-02-20 | Pane navigation | minor | No visual/audio feedback at navigation boundaries (`src/domain/open_chat_state.rs`) | When user reaches first/last message and presses j/k, selection stays in place silently | Consider adding status message or visual indicator when boundaries are reached | open |
| 2026-02-20 | Send message | medium | RwLock usage pattern in `send_message` should be documented (`src/telegram/auth.rs`) | Lock is correctly dropped before async block, but pattern must be maintained to prevent deadlocks | Add code comment explaining the pattern for future developers | wontfix — `src/telegram/auth.rs` removed in TDLib migration; TDLib send_message uses synchronous FFI |
| 2026-02-20 | Send message | minor | Stub backend returns `Unauthorized` instead of `Unavailable` for send_message (`src/telegram/mod.rs:229`) | Inconsistent with `list_chats` and `list_messages` which return `Unavailable` | Consider changing to `Unavailable` for consistency | open |
| 2026-02-20 | Send message | minor | No message length validation at usecase layer (`src/usecases/send_message.rs`) | MessageInputState enforces 4096 char limit at UI level, but usecase could be called directly | Consider adding length validation for defense in depth | open |
| 2026-02-21 | Chat list improvements | minor | Helper functions in auth.rs (`chat_type_from_grammers`, `is_user_online`, etc.) grow file beyond 200 LOC guideline (`src/telegram/auth.rs:376-457`) | File already 1218 lines; functions work correctly | Extract to `src/telegram/chat_helpers.rs` module | wontfix — `src/telegram/auth.rs` removed in TDLib migration; mapping now in `tdlib_mappers.rs` |
| 2026-02-21 | Chat list improvements | minor | No explicit test for `ChatType::Channel` rendering behavior (`src/ui/view.rs`) | Implementation is straightforward (returns empty prefix) | Add test case for channel chat type | open |
| 2026-02-21 | Chat list improvements | minor | Online status timestamp comparison casts u64 to i32 without comment (`src/telegram/auth.rs:391-396`) | Matches Telegram API format; year 2038 edge case | Add clarifying comment about Telegram timestamp format | wontfix — `src/telegram/auth.rs` removed in TDLib migration; TDLib handles status differently |
| 2026-02-21 | Chat list improvements | minor | Redundant `use` statements in test helper functions (`src/domain/chat_list_state.rs`, `src/usecases/list_chats.rs`, etc.) | Minor code duplication | Move `use` to top of test module | open |
| 2026-02-21 | Chat list improvements | minor | `OutgoingReadStatus.is_read` semantics unclear when `is_outgoing` is false (`src/domain/chat.rs:17-20`) | Documentation says "only meaningful" but code sets false | Clarify documentation or use `Option<bool>` | open |
| 2026-02-22 | Log rotation | medium | No validation for `max_log_files` edge cases (`src/infra/config/app_config.rs:14`) | `max_log_files = 0` deletes all logs making debugging impossible | Add validation to reject 0 or use `NonZeroUsize` | open |
| 2026-02-22 | Log rotation | minor | Silent error handling in `cleanup_old_logs` (`src/infra/logging.rs:70,86`) | Cleanup failures are silently ignored; debugging rotation issues is difficult | Add `eprintln` for errors (logger not initialized yet) | open |
| 2026-02-22 | Log rotation | minor | Test relies on timing for file ordering (`src/infra/logging.rs:107,153`) | `thread::sleep(10ms)` for different mtime is fragile in CI | Consider using `filetime` crate for deterministic tests | open |
| 2026-02-22 | Log rotation | minor | Weak test assertion for `build_env_filter` (`src/infra/logging.rs:181-191`) | Debug output check doesn't verify filter actually works | Test actual filtering behavior or verify filter string structure | open |
| 2026-02-24 | Advisory session lock | minor | `make_layout()` uses nanosecond timestamp which can collide under parallel test execution (`src/usecases/startup.rs:373`) | Pre-existing; the `logout_reset` test mutates `XDG_CONFIG_HOME` causing env-level interference with parallel tests | Use `tempfile::TempDir` (already a dev-dep) or add atomic counter to suffix | open |
| 2026-02-24 | Advisory session lock | minor | `fs2` crate last released in 2017; `std::fs::File::try_lock_exclusive` is on track for stabilization | The crate is effectively "done" — underlying syscalls are stable, ~47M downloads | Migrate to `std::fs::File::try_lock_exclusive` when stabilized (tracking: rust-lang/rust#130994) | planned |
| 2026-02-24 | Borderless panels | minor | Closure `block()` in `render_messages_panel` clones title String on each call (`src/ui/view.rs`) | Only one branch executes per render; cost is negligible at TUI refresh rates | No action needed; acceptable trade-off for readability | wontfix |
| 2026-02-24 | Borderless panels | minor | No tests for style composition / background inheritance between panel styles and child widget styles (`src/ui/styles.rs`) | Ratatui's style merging is well-tested upstream; low risk of regression | Add integration-level render tests if visual regressions appear | open |
| 2026-02-25 | Timestamp dedup | minor | Magic indent `"      "` (6 spaces) duplicated in 3 locations in `message_item()` and `build_message_header_line()` (`src/ui/message_rendering.rs`) | Pre-existing; changing time format would require updating all locations | Extract `TIME_INDENT` constant at module level | open |
| 2026-02-25 | Timestamp dedup | minor | `build_message_header_line` takes `sender: Option<&str>` but is always called with `Some` (`src/ui/message_rendering.rs`) | Pre-existing; function signature is slightly misleading | Change to `sender: &str` or keep for flexibility | open |
| 2026-02-25 | Timestamp dedup | minor | No rendering-level test for hidden time in `ListItem` output (`src/ui/message_rendering.rs`) | Rendering branches for `show_time: false` are only tested implicitly via element-level tests | Add test calling `element_to_list_item()` with `show_time: false` and assert blank span | open |
| 2026-02-25 | Message scroll fix | minor | `SCROLL_MARGIN` constant lives in domain layer but is a UI concern (`src/domain/open_chat_state.rs`) | Pragmatic placement near related state; only consumed by `ui::view` | Consider moving to `ui::constants` or `ui::view` module | open |
| 2026-02-25 | Message scroll fix | minor | `scroll_margin_constant_is_five` test is a tautological change detector (`src/domain/open_chat_state.rs`) | Validates constant exists but not behavior | Replace with range assertion or remove | open |
| 2026-02-25 | Message scroll fix | minor | `usize::MAX` used as magic sentinel for "scroll to bottom" in `scroll_offset` (`src/domain/open_chat_state.rs`) | Works correctly but latent fragility; arithmetic overflow could trigger unintended scroll-to-bottom | Refactor to `Option<usize>` or enum `ScrollPosition { BottomAlign, Offset(usize) }` | open |
| 2026-02-27 | Non-blocking UI | medium | No in-flight guard for `dispatch_load_messages` / `dispatch_send_message` (`src/usecases/shell.rs`) | Rapid key presses can spawn redundant threads; stale-result detection handles correctness but wastes resources | Add `messages_in_flight: Option<i64>` and `send_in_flight: bool` guards | open |
| 2026-02-27 | Non-blocking UI | medium | `BackgroundTaskResult` lives in domain layer but carries usecase-level semantics (`src/domain/events.rs`) | Variants like `MessageSent { original_text }` are UI-recovery concerns, not domain concepts | Move `BackgroundTaskResult` + `BackgroundError` to `usecases::background` and re-export from `AppEvent` via generic payload | open |
| 2026-02-27 | Non-blocking UI | medium | `Arc::get_mut().expect()` in guided auth is a runtime panic risk (`src/app.rs:56-57`) | If Arc has other clones, this panics; invariant is fragile | Replace with `.ok_or_else()` for recoverable error | open |
| 2026-02-27 | Non-blocking UI | medium | `build_logout_telegram` has redundant `drop(arc)` and silently falls back to stub on multiple owners (`src/app.rs:95-98`) | Logout may silently skip actual Telegram disconnect | Remove redundant drop; consider logging error instead of silent fallback | open |
| 2026-02-27 | Non-blocking UI | medium | Event polling only drains one background result per cycle (`src/ui/event_source.rs`) | Multiple results arriving simultaneously are processed across separate cycles | Consider drain-all pattern similar to connectivity events if latency becomes noticeable | open |
| 2026-02-27 | Non-blocking UI | minor | `StubTaskDispatcher.result_tx` and `inject_result` produce dead_code warnings (`src/usecases/background.rs:203,213`) | Test infrastructure prepared for future use but currently unused | Add `#[allow(dead_code)]` or use in at least one test | open |
| 2026-02-27 | Non-blocking UI | minor | `try_send_message` dispatches untrimmed text as `original_text` (`src/usecases/shell.rs`) | Restored text after failed send includes leading/trailing whitespace | Dispatch `trimmed.to_owned()` instead of `text.clone()` | open |
| 2026-02-27 | Non-blocking UI | minor | `BackgroundError` lacks `Display` and `Error` impls (`src/domain/events.rs`) | Limits ergonomic use in error chains and logging | Add `Display` impl | open |
| 2026-03-03 | TDLib Phase 2 | medium | `TelegramConfig` derives `Debug` and exposes `api_hash` in logs (`src/infra/config/app_config.rs`) | Inconsistent security posture with new `TdLibConfig` which redacts `api_hash` | Apply custom `Debug` to `TelegramConfig` to redact `api_hash` | open |
| 2026-03-03 | TDLib Phase 2 | medium | `TdLibClient::config()` returns `&TdLibConfig` exposing `api_hash` directly (`src/telegram/tdlib_client.rs:115-117`) | Any code with access to client can read raw `api_hash` value | Consider restricting visibility or providing targeted accessors for specific fields | open |
| 2026-03-03 | TDLib Phase 2 | medium | `TdLibConfig` derives `Clone` allowing easy proliferation of `api_hash` (`src/telegram/tdlib_client.rs:15-16`) | Multiple copies of sensitive data in memory | Evaluate `secrecy::SecretString` for `api_hash` in Phase 3+ if stricter secret handling is needed | open |
| 2026-03-03 | TDLib Phase 3 | medium | State caching race window in `take_or_wait_for_auth_state()` (`src/telegram/tdlib_auth.rs:142-147`) | Window between action and wait could miss fast TDLib responses | Drain pending states non-blocking before waiting | open |
| 2026-03-03 | TDLib Phase 3 | medium | `is_authorized()` taking `&mut self` is surprising for query method (`src/telegram/tdlib_auth.rs:150-165`) | Query semantics expected to be pure; 100ms timeout may cause false negatives | Split into `is_authorized_cached(&self)` and `refresh_authorization_status(&mut self)` | open |
| 2026-03-03 | TDLib Phase 3 | medium | `update_thread` handle is never joined (`src/telegram/tdlib_client.rs:82-83`) | Thread may outlive client if `close()` not called; no graceful shutdown | Implement thread joining in `close()` or `Drop` | open |
| 2026-03-03 | TDLib Phase 3 | minor | Lock poisoning error uses `TdLibError::Init` variant (`src/telegram/tdlib_client.rs:199-201`) | Semantically incorrect; not an initialization error | Add `TdLibError::Internal` variant for runtime concurrency errors | open |
| 2026-03-03 | TDLib Phase 3 | minor | Missing thread safety documentation for `TdLibClient` (`src/telegram/tdlib_client.rs:76-84`) | Users may not understand threading model | Add doc section on thread safety guarantees | open |
| 2026-03-03 | TDLib Phase 4 | medium | Sequential API calls in `list_chat_summaries()` (`src/telegram/tdlib_auth.rs:346-360`) | 50+ chats = 50-100+ blocking API calls causing slow load times | Batch or parallelize user lookups in `resolve_chat_metadata()` | open |
| 2026-03-03 | TDLib Phase 4 | medium | Thread safety: `&self` receiver with `block_on()` in `list_chat_summaries()` (`src/telegram/tdlib_auth.rs:329-363`) | Potential deadlock from async context; race conditions in concurrent calls | Document thread safety or add check to prevent async context calls | open |
| 2026-03-03 | TDLib Phase 4 | medium | Missing tests for `list_chat_summaries()` and `resolve_chat_metadata()` (`src/telegram/tdlib_auth.rs`) | Reduced confidence in correctness | Add unit tests for error handling and edge cases | open |
| 2026-03-03 | TDLib Phase 4 | minor | Unused parameter `_is_group_chat` in `extract_last_message_info()` (`src/telegram/tdlib_mappers.rs:73`) | Code clarity issue; suggests incomplete implementation | Remove parameter or implement intended functionality | open |
| 2026-03-03 | TDLib Phase 4 | minor | Silent skip on chat fetch failure (`src/telegram/tdlib_auth.rs:355-358`) | Users may not see chats with persistent fetch failures | Add failure threshold and return error if too many fail | open |
| 2026-03-03 | TDLib Phase 4 | minor | Missing error condition docs on `list_chat_summaries()` (`src/telegram/tdlib_auth.rs:326-332`) | API consumers may not handle errors appropriately | Add error documentation to doc comment | open |
| 2026-03-04 | TDLib Phase 5 | medium | Sequential user lookups in `list_messages()` (`src/telegram/tdlib_auth.rs`) | O(n) `get_user()` calls for n messages; slow for large message batches | Batch user lookups or cache resolved users | open |
| 2026-03-04 | TDLib Phase 6 | medium | Thread not joined on monitor drop (`src/telegram/chat_updates.rs`) | Monitor thread may outlive parent; no graceful shutdown | Join thread in `Drop` impl or on explicit stop | open |
| 2026-03-04 | TDLib Phase 6 | medium | Silent suppression of update send failures in update loop (`src/telegram/tdlib_client.rs`) | If update channel is full or closed, events are silently dropped | Log warning on send failure or use bounded channel with backpressure | open |
| 2026-03-04 | TDLib Phase 7 | medium | `logout_and_reset` removes TDLib data before calling `disconnect_and_reset()` (`src/usecases/logout.rs`) | TDLib may still have open file handles; could fail on Windows or cause writes to deleted paths | Reverse order: disconnect TDLib first, then remove data files | open |
| 2026-03-04 | TDLib Phase 7 | medium | Overly broad `"auth"` substring match in error mappers (`src/telegram/tdlib_auth.rs:595,609,627`) | Could misclassify non-authorization errors (e.g. "authentication code expired") as `Unauthorized` | Use more specific pattern like `"not authorized"` instead of `"auth"` | open |
| 2026-03-04 | TDLib Phase 7 | medium | `TdLibClient::close()` is never called in normal shutdown path (`src/telegram/tdlib_client.rs`, `src/telegram/tdlib_auth.rs:301-307`) | TDLib resources may not be properly flushed; `Drop` warning fires on every exit | Call `close()` in `disconnect_and_reset()` or in `TdLibAuthBackend::Drop` | open |
| 2026-03-04 | TDLib Phase 7 | medium | `set_tdlib_parameters` has misleading comment on 4th positional parameter (`src/telegram/tdlib_client.rs:324`) | Comment says "files_directory (deprecated)" but the 3rd param is already `files_directory` | Verify actual TDLib API parameter name and update comment | open |
| 2026-03-04 | TDLib Phase 7 | medium | `tdlib_auth.rs` at 687 LOC exceeds 200 LOC soft limit (`src/telegram/tdlib_auth.rs`) | Mixes auth flow with data access (list_chats, list_messages, send_message) | Extract error mappers to `tdlib_error_mappers.rs` and data access to `tdlib_data_access.rs` | done |
| 2026-03-04 | TDLib Phase 7 | medium | `tdlib_client.rs` at 601 LOC exceeds 200 LOC soft limit (`src/telegram/tdlib_client.rs`) | Contains lifecycle, update loop, all API wrappers, and tests | Extract update loop to `tdlib_updates.rs` or API wrappers to separate module | done |
| 2026-03-04 | TDLib Phase 7 | medium | `tdlib_mappers.rs` at 552 LOC exceeds 200 LOC soft limit (`src/telegram/tdlib_mappers.rs`) | Test fixtures alone account for ~200 LOC | Extract test fixtures to shared test support module | done |
| 2026-03-04 | TDLib Phase 7 | medium | `#[allow(unused_imports)]` on re-exports in `telegram/mod.rs` (`src/telegram/mod.rs:12-15`) | Dead public exports with suppressed warnings | Remove re-exports or remove `#[allow]` and add usage comment | open |
| 2026-03-04 | TDLib Phase 7 | minor | `GuidedAuthReason` enum has only one variant `Missing` (`src/usecases/startup.rs`) | Over-engineered for single variant but provides extensibility | Add comment explaining why kept as enum | open |
| 2026-03-04 | TDLib Phase 7 | minor | `#[allow(dead_code)]` on entire `TdLibUpdate` enum but all variants are constructed (`src/telegram/tdlib_updates.rs:11`) | Blanket suppression hides future truly unused variants | Remove enum-level `#[allow]`; keep only on `chat_id()` method | open |
| 2026-03-04 | TDLib Phase 7 | minor | Startup tests use manual temp dirs without cleanup (`src/usecases/startup.rs`) | Temp directory accumulation over many test runs | Use `tempfile::tempdir()` (already a dev-dep) for automatic cleanup | open |
| 2026-03-04 | TDLib Phase 7 | minor | `parse_flood_wait_seconds` lowercases internally but all callers already pass lowercased strings (`src/telegram/tdlib_auth.rs:572-585`) | Minor redundant allocation | Document expectation or skip internal lowercasing | open |
| 2026-03-14 | Chat list 404 fix | minor | Error mappers (`map_list_chats_error`, `map_messages_error`, `map_send_message_error`) use string matching instead of TDLib error codes (`src/telegram/tdlib_auth.rs`) | `code` field now available on `TdLibError::Request`; codes are more stable than message strings | Use `code` field (e.g., `401` for unauthorized) in addition to string matching | open |
| 2026-03-14 | Chat list 404 fix | minor | `map_tdlib_error` discards TDLib error code when mapping `Request` to `AuthBackendError::Transient` (`src/telegram/tdlib_auth.rs:499-513`) | Loses diagnostic information for auth-related failures | Include TDLib code in transient error message or add numeric field to `AuthBackendError` | open |
| 2026-03-14 | Chat list 404 fix | minor | Repetitive `map_err` closures for `TdLibError::Request` conversion (`src/telegram/tdlib_client.rs`) | Same pattern appears 8 times | Add `From<tdlib_rs::types::Error> for TdLibError` impl to deduplicate | open |
| 2026-03-14 | Message pagination | medium | `tdlib_auth.rs` at ~790 LOC exceeds 200 LOC soft limit — mixes auth, chat listing, messages, sending, and 7 error mappers | File keeps growing with each feature | Extract error mappers to `tdlib_error_mappers.rs` and data ops to `tdlib_operations.rs` | done |
| 2026-03-14 | Message pagination | medium | Mid-pagination error discards already-fetched messages (`src/telegram/message_pagination.rs`) | Transient network error on page 2+ returns error instead of partial results | Consider returning partial results when first page succeeded and later pages fail | open |
| 2026-03-14 | Message pagination | medium | `openChat` failure silently swallowed at `warn` level (`src/telegram/tdlib_auth.rs`) | If `openChat` fails, TDLib may not preload history — potentially not fixing the original bug | Promote to `error` level; document why continuing is acceptable (`only_local=false` still fetches) | open |
| 2026-03-14 | Message pagination | minor | `tdlib_client.rs` at ~754 LOC after adding `open_chat`/`close_chat` | Ongoing growth | Track for decomposition alongside Phase 7 backlog entry #91 | open |
| 2026-03-14 | Cached chat list startup | medium | `get_chat()`/`get_user()` in cache-only path may trigger network requests if chat data not fully cached (`src/telegram/tdlib_auth.rs`) | On cold cache or partial DB, synchronous startup could block on network I/O | Add timeout guard around cache preload in `compose_shell()` and document the assumption | open |
| 2026-03-14 | Cached chat list startup | minor | `ConnectivityStatus::Connecting` shown alongside `Ready` chat list from cache | Technically correct (app is connecting), but may look odd to users | Consider distinct UI state for "showing cached data while updating" | open |
| 2026-03-14 | Cached messages on open | medium | No in-flight guard for `dispatch_load_messages` — rapid reopen of same Loading chat spawns duplicate background threads (`src/usecases/shell.rs`) | Stale-result guard prevents wrong data, but wastes resources | Add `messages_in_flight: Option<i64>` guard similar to `chat_list_in_flight` | open |
| 2026-03-14 | Cached messages on open | medium | `try_show_cached_messages` calls `set_loading` then immediately `set_ready` — unnecessary intermediate state transition (`src/usecases/shell.rs:140-167`) | No functional bug (intermediate state never rendered), but semantically misleading | Extract `set_chat_identity(chat_id, title)` method on `OpenChatState` to set ID/title without full Loading transition | open |
| 2026-03-14 | Cached messages on open | minor | Verbose type annotation for `cache_source` binding in `compose_shell()` (`src/usecases/bootstrap.rs`) | Readability; type is inferable from context | Simplify with `use` import and let type inference work | open |
| 2026-03-14 | Cached messages on open | minor | Missing test for background error arriving on cache-Ready chat (`src/usecases/shell.rs`) | Current behavior: error replaces cached messages; test would document intent | Add test asserting `OpenChatUiState::Error` when background load fails on cached-Ready chat | open |
| 2026-03-15 | Chat list UI polish | minor | Add combined test for outgoing-status + unread-badge + online-indicator suffix | Verify suffix_len arithmetic when all three suffix components are present | Add test with `is_outgoing: true`, `unread_count > 0`, `is_online: Some(true)` | open |
| 2026-03-15 | Chat list UI polish | minor | Consider extracting chat list rendering into `src/ui/chat_list_rendering.rs` | `view.rs` at ~1100 LOC (including tests) exceeds 200 LOC soft limit | Extract `chat_list_item_line`, prefix/suffix builders, `PrefixSegment` to dedicated module | done |
| 2026-03-15 | Unicode width fix | medium | `message_input.rs` cursor positioning uses character index, not display width | Emoji/CJK in message input will misplace the terminal cursor | Use `unicode-width` in cursor position calculation or expose display-width cursor from `MessageInputState` | open |
| 2026-03-15 | Unicode width fix | minor | Ambiguous-width character `\u{2022}` (bullet) may render as 2 cells in CJK terminals | CJK locale terminal emulators may show wider bullet, causing overflow | Document as known limitation; consider detection if CJK users report issues | open |
| 2026-03-19 | Chat view improvements | medium | `view.rs` at ~1272 LOC exceeds 200 LOC soft limit; chat list rendering is a cohesive unit | Hard to navigate and maintain | Extract chat list rendering into `src/ui/chat_list_rendering.rs` | done |
| 2026-03-19 | Chat view improvements | minor | `sender_name_style(name, is_outgoing)` ignores `name` when `is_outgoing=true` | Slightly misleading API; semantically correct | No change needed; doc comment already clarifies | wontfix |
| 2026-03-19 | Chat view improvements | minor | Cyan used for both outgoing sender (`sender_name_style`) and group sender in chat list (`group_sender_style`) | Minor visual overlap between different contexts | No change needed; contexts are separate (chat list vs message panel) | wontfix |
| 2026-03-19 | Mark as read | medium | `shell.rs` at ~480 non-test LOC exceeds 200 LOC soft limit; lifecycle methods form cohesive group | Growing module complexity | Extract chat lifecycle orchestration into `usecases/chat_open_flow.rs` | open |
| 2026-03-19 | Mark as read | medium | Race condition: `dispatch_open_chat` and `dispatch_load_messages` spawn independent threads with no ordering guarantee | Thread scheduling may cause `getChatHistory` to execute before `openChat`; TDLib internal serialization likely prevents issues in practice | Document ordering assumption; consider chaining `open_chat` + `load_messages` in single thread for guaranteed ordering | open |
| 2026-03-19 | Mark as read | minor | `ChatLifecycleError::ChatNotFound` variant defined but never constructed | Prepared for future use but currently dead code | Remove if not needed soon, or document planned use case | open |
| 2026-03-19 | Chat sub-item scrolling | medium | `scroll_padding` is measured in items but applied as lines in `ensure_selected_visible` | With very tall messages and `SCROLL_MARGIN=5`, padding zone could exceed viewport height causing scroll jitter | Cap effective scroll padding to `viewport_height / 3` | open |
| 2026-03-19 | Chat sub-item scrolling | medium | `chat_message_list.rs` at ~740 LOC exceeds 200 LOC soft limit | Clear internal sections map to sub-modules: state, offset, render, tests | Split into `chat_message_list/` directory with `mod.rs`, `state.rs`, `offset.rs`, `render.rs` | done |
| 2026-03-19 | Chat sub-item scrolling | minor | `block.clone()` in `StatefulWidget::render` for `ChatMessageList` | Block is behind `&` reference but `render` takes `self`; could move block out of self | Restructure to move block out of self instead of cloning | open |
| 2026-03-19 | Chat sub-item scrolling | minor | Missing test for zero-height items in `render_items` | Handler exists (`continue` on `visible_lines_in_item == 0`) but not explicitly tested | Add test with empty `Text` item | open |
| 2026-03-19 | Chat sub-item scrolling | minor | `OpenChatUiState` could derive `Copy` to avoid `.clone()` in getter | Fieldless enum is `Copy`-safe; currently cloned in `ui_state()` | Derive `Copy` and remove `.clone()` | open |
| 2026-03-20 | Visual tweaks | minor | `is_bot: bool` on `ChatSummary` uses `bool` where `Option<bool>` would be more consistent with `is_online` | `false` conflates "not a bot" with "not applicable (group/channel)" | Change to `Option<bool>` for consistency with `is_online` semantics | open |
| 2026-03-20 | Dynamic message updates | minor | O(n²) dedup in `ChannelChatUpdatesSignalSource::pending_update_chat_ids` uses `Vec::contains` | Quadratic with many distinct chat_ids in a burst; negligible for typical counts (<100) | Replace with `HashSet<i64>` if profiling shows impact | open |
| 2026-03-23 | Voice recording | medium | Missing orchestrator tests for voice recording flow (`src/usecases/shell.rs`) | No tests for `start_voice_recording`, `handle_command_popup_key`, `handle_command_exited`, `send_voice_recording`, `discard_voice_recording` | Add tests for key state transitions before merging to main | done |
| 2026-03-23 | Voice recording | medium | `ReaderGate::on_reader_finished` always sends `success: true` (`src/usecases/voice_recording.rs`) | Actual exit status is not captured from the child process | Fix when error handling for failed recordings is added | open |
| 2026-03-23 | Voice recording | minor | `command_popup.rs` clones strings in render via `line.to_owned()` | New String per visible line per frame (~200 clones/sec) | Accept as pragmatic trade-off for ratatui lifetime ergonomics | wontfix |
| 2026-03-23 | Voice recording | minor | `ShellOrchestrator` trait has command-popup-specific `take_pending_command_rx` method | Every implementor must implement it; surface area grows | Consider generic side-effect mechanism if more feature-specific methods accumulate | open |
| 2026-03-23 | Voice recording | minor | `send_voice.rs` is very thin (26 lines) | Trait + Arc blanket impl only | Co-locate with `send_message.rs` or keep separate if voice sending grows | open |
| 2026-03-23 | Voice recording tests | minor | `stop_voice_recording` not exercised in unit tests (recording_handle always None) | Cannot spawn real ffmpeg in tests; Drop handles cleanup | Cover in future integration tests | open |
| 2026-03-23 | Voice recording tests | minor | Test temp files use hardcoded names under `std::env::temp_dir()` | Stale files from crashed runs could interfere; cleanup is best-effort | Consider `tempfile` crate for automatic cleanup | open |
| 2026-03-23 | Voice recording config | medium | `CommandEvent::Exited { success }` field is always `true` — now vestigial since exit code is checked via `try_exit_success()` | Misleading signal; future devs may rely on wrong value | Remove `success` field from `CommandEvent::Exited` (make it unit variant) or propagate real exit code | open |
| 2026-03-23 | Voice recording config | medium | `split_whitespace` command parsing in `start_recording` is fragile for user-provided commands with quoted args | Breaks for device names with spaces (e.g. `'Built-in Microphone:none'`) | Use `shell-words` crate for proper shell-like argument parsing | open |
| 2026-03-23 | Voice recording config | medium | No validation that user-provided `record_cmd` contains `{file_path}` placeholder | Silent failure: command runs but output goes nowhere | Add warning log or validation in `FileVoiceConfig::merge_into` | open |
| 2026-03-23 | Voice recording config | minor | Hardcoded `~/.config/rtg/config.toml` path in error message (`shell.rs`) | Misleading if user uses `--config` custom path | Consider making error message generic or passing actual config path | open |
| 2026-03-23 | Voice recording config | minor | `thread::sleep(50ms)` in tests for process exit detection | Fragile under heavy CI load | Use `child.wait()` for deterministic tests | open |
| 2026-03-23 | Optimistic voice UI | medium | `remove_pending_messages` removes ALL pending messages, not just voice | Concurrent text+voice sends: failure in one rolls back the other's optimistic display | Consider `remove_pending_by_media(MessageMedia)` or unique pending IDs | open |
| 2026-03-23 | FD isolation | minor | FD closing loop upper bound of 1024 may miss high FDs | Crossterm typically opens low FDs; 1024 covers realistic scenarios | Consider `libc::getrlimit(RLIMIT_NOFILE)` for exact upper bound | open |
| 2026-03-23 | Message open | medium | `recording_handle`/`recording_file_path`/`pending_command_rx` shared for recording and playback | No bug while popup mutex prevents coexistence, but names are misleading | Rename to `command_handle`/`command_file_path`/`command_rx` or introduce `ActiveCommand` enum | open |
| 2026-03-23 | Message open | medium | Silent failure when `l` pressed on not-yet-downloaded file | User gets no feedback; only tracing::info log | Add user-visible status bar message "File not downloaded yet" | open |
| 2026-03-23 | Message open | minor | `voice_recording.rs` module name misleading after `start_command` generalization | Module now handles both recording and playback command spawning | Rename to `command_runner.rs` or `external_command.rs` | open |
| 2026-03-23 | Message open | minor | `new_with_initial_state` takes 8 args, suppressed via `#[allow(clippy::too_many_arguments)]` | Constructor accumulates config concerns | Introduce `OrchestratorConfig` struct to group config fields | open |
| 2026-03-23 | Message open | minor | No tests for malformed MIME types in `resolve_open_command` | e.g. `"audio"` (no slash), `"/"`, `"audio/ogg/extra"` | Add edge-case tests | open |
| 2026-03-23 | File download | medium | `ChatUpdate::FileUpdated` carries TDLib-shaped fields in domain layer | Leaks `is_downloading_active`/`is_downloading_completed` booleans instead of `DownloadStatus` | Refactor to use domain `DownloadStatus` in `ChatUpdate::FileUpdated`, move derivation logic to telegram layer | open |
| 2026-03-23 | File download | medium | `new_with_initial_state` now takes 9 args with `max_auto_download_bytes` | Constructor accumulates config concerns | Introduce `OrchestratorConfig` struct to group config fields | open |
| 2026-03-23 | File download | minor | Photo MIME type hardcoded to `"image/jpeg"` in `extract_photo_file_info` | TDLib `PhotoSize` doesn't expose MIME type; most photos are JPEG | Accept limitation; document in code comment | done |
| 2026-03-23 | File download | minor | `append_file_meta_to_media_line` uses bracket detection that could match non-media content | Only called when `file_meta` is `Some`, limiting false positives | Could be made more robust by checking specific media labels | open |
| 2026-03-23 | Command popup PTY output | medium | `CommandEvent::Exited` is emitted on reader completion, not guaranteed child termination (`src/usecases/voice_recording.rs`, `src/usecases/shell.rs`) | If output FDs close before process exit, orchestrator may treat `try_exit_success()==None` as failure | Emit Exited from actual process wait status (or add dedicated process-wait event) instead of reader-gate completion | open |
| 2026-03-23 | Command popup PTY output | minor | FD-closing loop uses hardcoded `3..1024` range in pre-exec (`src/usecases/voice_recording.rs`) | High-numbered inherited FDs above 1023 can remain open on systems with larger `RLIMIT_NOFILE` | Use dynamic upper bound (`getrlimit`/`sysconf`) or `close_range` where available | open |
| 2026-03-23 | Status bar redesign | minor | `ConnectivityStatus::as_label()` is now unused from production code | Only consumed in tests; suppressed with `cfg_attr(not(test), allow(dead_code))` | Remove method if no call sites remain, or add direct test exercising it | open |
| 2026-03-23 | Status bar redesign | minor | Removing `PartialEq/Eq` from `ShellState` due to `Instant` field is broad | Limits ability to compare states in tests | Consider manual `PartialEq` impl excluding transient timestamp, or wrap timestamp in comparable newtype | open |
| 2026-03-24 | Chat info popup | medium | Telegram chat-info resolution swallows most TDLib errors and returns "successful" ChatInfo with empty/zero fields (`src/telegram/tdlib_auth.rs:714-880`) | UI may show misleading values (0 members, empty status) instead of clear failure | Return `Err(ChatSubtitleError::Unavailable)` for failed critical fetches; keep fallback only where intentionally degraded | open |
| 2026-03-24 | Media file open | minor | Footer text "Press q to stop" does not mention Esc which also closes Playback/Viewer popups (`src/ui/command_popup.rs:60`) | Discoverability of Esc close is low | Render footer text by both phase and popup kind to reflect actual key handling | open |
| 2026-03-24 | Media file open | minor | `is_platform_default()` uses exact string equality against `DEFAULT_OPEN` (`src/domain/open_defaults.rs:18`) | Extra whitespace or equivalent commands won't match | Compare normalized executable token (`open`/`xdg-open`) after splitting instead of full template string | open |
| 2026-03-25 | Reply to message | medium | Reply context is not restored on failed send (`src/usecases/shell.rs`) | Retry after failure may send plain message without reply linkage | Extend `BackgroundTaskResult::MessageSent` recovery payload to restore both text and reply context | open |
| 2026-03-25 | Reply to message | medium | Input reply preview truncation is char-count based, not display-width aware (`src/ui/message_input.rs`) | Emoji/CJK can overflow preview width and misalign visual layout | Switch preview truncation to `unicode-width` logic, aligned with `message_rendering` | open |
| 2026-03-25 | Reply to message | medium | Some TDLib reply payloads can produce empty reply preview (`src/telegram/tdlib_mappers.rs`, `src/ui/message_rendering.rs`) | UI may render only `│` without meaningful sender/text on sparse metadata | Add fallback placeholders (`Unknown`, `[Message]`) before rendering | open |
| 2026-03-25 | Reply to message | minor | Missing unit test for `SendMessageCommand.reply_to_message_id` pass-through (`src/usecases/send_message.rs`) | Regression risk in send pipeline wiring | Add test asserting `MessageSender::send_message` receives `Some(reply_id)` | open |
| 2026-03-25 | Reply to message | minor | Missing dedicated tests for reply enrichment helper (`src/telegram/tdlib_auth.rs`) | Mapping edge cases (quote fallback, same-chat backfill) may regress silently | Add unit tests for `extract_reply_info` + `enrich_same_chat_reply_info` scenarios | open |
| 2026-03-25 | Merge main into reaction branch | medium | No test covers message rendering when both `reply_to` and `reaction_count > 0` are present (`src/ui/message_rendering.rs`) | Conflict resolution touched both rendering paths; gap can hide regressions in combined layout | Add a rendering test for reply + reaction in one message (header/reply/content/reaction placement) | open |
| 2026-03-25 | Merge main into reaction branch | minor | Reply preview truncation in input uses char-count instead of display width (`src/ui/message_input.rs`) | Emoji/CJK may truncate and align incorrectly in the reply preview line | Switch truncation to `unicode-width` based logic | open |
| 2026-03-28 | shell.rs refactoring | medium | Tests (~4900 LOC) remain in `shell/mod.rs` — not yet extracted into `shell/tests/` submodules | Navigation and locality suffers; inconsistent with refactoring guide | Move tests to `shell/tests/` with per-module subfiles | done |
| 2026-03-28 | shell.rs refactoring | medium | `OrchestratorCtx` is a broad mutable context with 14 pub fields | Reintroduces coupling risk similar to large `self`; all modules see all fields | Split into narrower concern-focused contexts (ChatCtx, MediaCtx, etc.) or use private fields with accessors | open |
| 2026-03-28 | shell.rs refactoring | minor | `chat_updates.rs` mixes push update routing and file download state transitions | Slightly blurs responsibility boundary | Extract `file_updates.rs` for download-specific logic | open |
| 2026-03-28 | shell.rs refactoring | minor | `voice.rs` contains non-voice playback/viewer popup orchestration | Name implies voice-only but handles generic command popup lifecycle | Rename to `media_commands.rs` or split into voice + playback submodules | open |
| 2026-03-28 | message_rendering refactoring | medium | `tests/rendering.rs` (386 LOC) and `tests/element_building.rs` (362 LOC) exceed soft limit | Large test files reduce navigability | Split into smaller focused test files (e.g. status_rendering, media_rendering, time_grouping, index_mapping) | open |
| 2026-03-28 | message_rendering refactoring | medium | `tests/text_utils.rs` and `tests/content_spans.rs` call internal `pub(super)` functions directly | Conflicts with "tests call public API" guide rule; acceptable for pure utility unit tests | Keep as pragmatic exception for deterministic pure-function testing; document rationale | wontfix |
| 2026-03-28 | view.rs refactoring | medium | `compute_visual_index` assumes pinned chats occupy first contiguous segment of `chats` slice (`src/ui/view/chat_list.rs`) | Pre-existing; if upstream provides mixed pinned/regular ordering, visual index mapping will be incorrect | Compute visual index based on partition position, not raw slice index | open |
| 2026-03-28 | event_source/chat_message_list refactoring | minor | Tests in `offset.rs`/`key_mapping.rs` call internal `pub(super)` functions directly instead of exercising public API | Acceptable for pure-function unit tests; provides deterministic isolated coverage | Keep as pragmatic exception for pure utility testing per guide flexibility | wontfix |
| 2026-03-28 | shell tests refactoring | minor | `voice/command_lifecycle.rs` (319 LOC) and `voice/send_and_optimistic.rs` (364 LOC) slightly exceed ~250 LOC target | Still much better than 937 LOC monolith; further splits would create very small files | Consider splitting if files grow further (e.g. separate command output vs phase transitions, e2e flows vs optimistic UI) | open |
| 2026-03-28 | shell tests refactoring | minor | `src/usecases/shell/tests/mod.rs` (426 LOC) is the largest shared fixture hub | Centralized growth point for test doubles, factories, and helpers | Consider extracting shared test infra into submodules (`fixtures.rs`, `dispatcher.rs`, `factories.rs`) | open |
| 2026-03-30 | Chat list refresh fix | medium | Missing regression tests for `build_summaries_from_ids` cache-first + TDLib-fallback logic | Method requires real TDLib client, hard to unit test; cache-first lookup order is performance-critical and should be guarded | Add mock-based tests verifying: cache hit skips `get_chat`, cache miss calls `get_chat` and backfills cache, all-miss returns empty, `list_chat_summaries` returns `Unavailable` when all chats fail | open |
| 2026-03-30 | Chat list refresh fix | minor | Per-chat `warn!` on double-miss (cache + TDLib) may create noisy logs in pathological cases | Cold cache + transient TDLib failures could produce many warnings | Aggregate unresolved count into single `warn!` summary; use `debug!` per individual chat | open |
